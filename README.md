

```md
# 🧩 Golang + PostgreSQL CRUD API

یک پروژه ساده و استاندارد برای پیاده‌سازی **CRUD** روی یک مدل کاربر (User) با استفاده از:
- Golang  
- Gin Framework  
- PostgreSQL (pgx driver)

این پروژه با ساختار تمیز و لایه‌بندی شده طراحی شده تا برای یادگیری و توسعه واقعی مناسب باشد.

---

## 📦 ویژگی‌ها

- اتصال پایدار به PostgreSQL با `pgx`
- پیاده‌سازی CRUD کامل:
  - ساخت کاربر (Create)
  - دریافت همه کاربران (Get All)
  - دریافت کاربر با ID (Get By ID)
  - آپدیت کاربر (Update)
  - حذف کاربر (Delete)
- استفاده از Repository Pattern
- روتینگ تمیز با Gin
- هندلرهای جداگانه برای هر روت

---

## 🗂 ساختار پروژه

```

/pgsql
│── main.go
│── go.mod
│
├── handler/
│   └── user_repo.go
│
├── router/
│   └── user_router.go
│
└── model/
└── user.go

````

---

## 🛢 ساخت جدول کاربران

پیش از اجرای پروژه مطمئن شوید جدول زیر ساخته شده است:

```sql
CREATE TABLE users (
    id SERIAL PRIMARY KEY,
    name VARCHAR(100),
    email VARCHAR(150),
    age INT
);
````

---

## 🔌 تنظیمات دیتابیس

اتصال در `main.go` انجام شده:

```go
dns := "postgres://postgres:123456@localhost:5432/postgres?sslmode=disable"
db, err := sql.Open("pgx", dns)
```

برای استفاده در محیط واقعی:

* یوزر، پسورد، پورت، نام دیتابیس را تغییر دهید.
* از متغیر محیطی (ENV) استفاده کنید.

---

## 🚀 اجرای پروژه

```bash
go mod tidy
go run main.go
```

---

## 📡 Endpoint ها

### ➕ ایجاد کاربر

**POST** `/addUser`
Body:

```json
{
  "name": "Ian",
  "email": "ian@example.com",
  "age": 25
}
```

---

### 📥 دریافت همه کاربران

**GET** `/getAll`

---

### 🔍 دریافت کاربر با ID

**GET** `/getUserById/:id`

مثال:

```
GET /getUserById/2
```

---

### ✏️ آپدیت کاربر

**PUT** `/updateUser/:id`

Body:

```json
{
  "name": "updated",
  "email": "new@email.com",
  "age": 44
}
```

---

### 🗑 حذف کاربر

**DELETE** `/deleteUser/:id`

---

## 🧪 تست با Postman / Thunder Client

تمام روت‌ها آماده تست هستند.
کافیست URL لوکال:

```
http://localhost:5010
```

---

## 🏁 نتیجه

این پروژه یک **پایه قوی** برای ساخت سرویس‌های واقعی Go + PostgreSQL است و به راحتی می‌تواند توسعه داده شود:

* اضافه کردن middleware
* JWT auth
* migration tools
* docker compose
* service layer
